export const environment = {
  production: false,
  apiUrl: '',       // desarrollo: URLs relativas, las maneja proxy.conf.json
  socketUrl: '',    // desarrollo: socket.io en la misma origen (vía proxy)
};
